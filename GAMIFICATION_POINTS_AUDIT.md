# فحص نظام النقاط (Gamification) — هل هو مفعّل تلقائياً؟

## الملخص

| النشاط | مفعّل؟ | ملاحظات |
|--------|--------|---------|
| **إتمام درس** | ✅ نعم | يُطلق عند تحديد الدرس كمكتمل من الطالب |
| **إتمام اختبار** | ✅ نعم | يُطلق عند تسليم الاختبار |
| **إتمام كورس** | ✅ نعم | يُطلق عند وصول إكمال الدورة إلى 100% |
| **تسليم واجب** | ✅ نعم | يُطلق عند تسليم الواجب |
| **مشاهدة فيديو (80%+)** | ❌ لا | الحدث غير مُطلق من أي مكان + عدم تطابق خصائص الحدث مع الـ Listener |
| **تسجيل الدخول اليومي** | ✅ نعم (تقنياً) | مُسجّل في GamificationServiceProvider على حدث Login |

---

## التفاصيل

### 1. التسجيل (الأحداث والـ Listeners)

- **GamificationServiceProvider** يسجّل في `boot()`:
  - `QuizCompleted` → `QuizCompletedListener`
  - `LessonCompleted` → `LessonCompletedListener`
  - `AssignmentSubmitted` → `AssignmentSubmittedListener`
  - `CourseCompleted` → `CourseCompletedListener`
  - `VideoWatched` → `VideoWatchedListener`
  - `Login` (Auth) → `UserLoginListener`
- الـ Provider موجود في `app/Providers` ويُحمّل تلقائياً في Laravel 11.

### 2. إطلاق الأحداث من الكود

| الحدث | أين يُطلق | الملف / السطر |
|-------|-----------|----------------|
| **LessonCompleted** | عند تحديد الدرس كمكتمل | `CourseLearningController::markModuleComplete()` ~226 |
| **CourseCompleted** | عند وصول إكمال الدورة 100% | `CourseLearningController::markModuleComplete()` ~244 |
| **QuizCompleted** | عند تسليم محاولة الاختبار | `QuizAttemptController` ~804 |
| **AssignmentSubmitted** | عند إنشاء تسليم واجب | `AssignmentSubmissionController` ~235 |
| **VideoWatched** | **لا يُطلق في أي مكان** | — |
| **Login** | تلقائي من Laravel عند تسجيل الدخول | — |

### 3. إعدادات النقاط (config/gamification.php)

- **إتمام درس:** 50 نقطة، 25 XP  
- **إتمام اختبار:** 100 نقطة أساسية مع مضاعف حسب الدرجة (ممتاز/جيد/نجاح/رسوب)، + مكافأة درجة كاملة 200 نقطة عند 100%  
- **إتمام كورس:** 1000 نقطة، 500 XP  
- **تسليم واجب:** 150 نقطة، 75 XP  
- **مشاهدة فيديو:** 30 نقطة، 15 XP (للـ 80%+ فقط) — لكن الحدث غير مُطلق  
- **تسجيل الدخول اليومي:** 10 نقاط، 5 XP  

لا يوجد إعداد عام لـ "تعطيل النقاط"؛ النظام يعمل حسب الأحداث والـ Listeners المسجّلة.

### 4. مشكلة مشاهدة الفيديو

- **الحدث `VideoWatched`** لا يُستدعى من:
  - `CourseLearningController::trackVideoProgress()` (يحدّث فقط `ModuleCompletion` ولا يطلق أي حدث).
  - أي مكان آخر في المشروع.
- **الـ Listener** يستخدم:
  - `$event->watchPercentage`
  - `$event->watchTime`
- بينما **الحدث `VideoWatched`** يعرّف فقط:
  - `$user`, `$video`, `$watchDuration`
- أي أن هناك **عدم تطابق** بين خصائص الحدث وما يتوقعه الـ Listener، وحتى لو أُطلق الحدث لاحقاً دون تعديل، سيحدث خطأ (خصائص غير معرفة).

**الخلاصة:** نقاط مشاهدة الفيديو **غير مفعّلة** حالياً (لا إطلاق للحدث + عدم تطابق الخصائص).

---

## ما يعمل تلقائياً حالياً

- عند **إتمام درس** → نقاط + XP حسب الإعداد (مع مضاعف السلسلة إن وُجد).
- عند **إتمام اختبار** → نقاط + XP (مع مضاعف حسب الدرجة) + مكافأة درجة كاملة إن كانت 100%.
- عند **إتمام كورس** → نقاط + XP.
- عند **تسليم واجب** → نقاط + XP.
- عند **تسجيل الدخول** → نقاط تسجيل الدخول اليومي (إذا كان المنطق في UserLoginListener يمنع التكرار أكثر من مرة في اليوم).

---

## ما لا يعمل / يحتاج تعديل

1. **مشاهدة الفيديو (80%+):**
   - إما استدعاء `VideoWatched::dispatch()` من مكان مناسب (مثلاً عند وصول نسبة المشاهدة إلى 80% أو 90% في `trackVideoProgress` أو في `ProgressTrackingService::trackVideoProgress`).
   - وتعديل حدث `VideoWatched` ليشمل الخصائص التي يستخدمها الـ Listener (`watchPercentage`, `watchTime`) أو تعديل الـ Listener ليتوافق مع الخصائص الحالية (`watchDuration` + حساب النسبة من مدة الفيديو).

2. **منع منح النقاط أكثر من مرة لنفس النشاط (اختياري):**
   - حالياً لا يوجد تحقق من "هل سبق منح نقاط لهذا المستخدم لهذا الدرس/الاختبار/الواجب". إذا أمكن تنفيذ نفس الإكمال أو التسليم أكثر من مرة، قد تُمنح النقاط أكثر من مرة. يمكن إضافة تحقق في `GamificationService` أو في `PointsService` (مثلاً وجود معاملة سابقة بنفس `source` + `related_type` + `related_id` للمستخدم) لعدم المنح المكرر إن رغبت.

---

## الخلاصة النهائية

- **نعم:** نظام النقاط **مفعّل تلقائياً** للطالب عند **إتمام الدروس، إتمام الاختبارات، إتمام الكورس، وتسليم الواجبات** (وتسجيل الدخول إن كان المنطق يطبّق ذلك).
- **لا:** **مشاهدة الفيديو** لا تمنح نقاطاً تلقائياً حالياً؛ الحدث غير مُطلق وهناك عدم تطابق بين حدث `VideoWatched` والـ Listener.

إذا رغبت، يمكن وضع خطوات تنفيذية محددة (تعديلات كود) لتفعيل نقاط الفيديو وتوحيد خصائص الحدث مع الـ Listener، أو لإضافة منع المنح المكرر لنفس الدرس/الاختبار/الواجب.

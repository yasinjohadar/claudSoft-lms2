# دليل إعداد WhatsApp Web - خطوة بخطوة

## نظرة عامة

بعد نجاح عملية الربط مع WhatsApp Web، سيكون النظام جاهزاً لإرسال الرسائل عبر جهازك الشخصي. هذا الدليل يشرح كيفية الإعداد والاستخدام.

## الخطوات الأساسية

### 1. إعداد Node.js Service

#### أ. تثبيت Dependencies
```bash
cd whatsapp-web-service
npm install
```

#### ب. إعداد ملف .env
انسخ `env.example` إلى `.env`:
```bash
cp env.example .env
```

عدّل ملف `.env`:
```env
PORT=3000
NODE_ENV=development
API_TOKEN=your-secret-api-token-here
SESSION_STORAGE_PATH=./sessions
```

**مهم:** احفظ `API_TOKEN` - ستحتاجه في Laravel.

#### ج. تشغيل الخدمة
```bash
# Development
npm run dev

# Production
npm start

# أو باستخدام PM2 (موصى به)
pm2 start server.js --name whatsapp-web-service
pm2 save
```

### 2. إعداد Laravel

#### أ. اختيار WhatsApp Web كمزود
1. اذهب إلى: **WhatsApp → الإعدادات العامة**
2. اختر **"WhatsApp Web (QR Code)"** من قائمة المزود
3. احفظ الإعدادات

#### ب. إعدادات WhatsApp Web
1. اذهب إلى: **WhatsApp → WhatsApp Web**
2. أدخل:
   - **رابط Node.js Service:** `http://localhost:3000` (أو الرابط الخاص بك)
   - **API Token:** نفس الـ token من ملف `.env` في Node.js service
3. اضغط **"اختبار الاتصال"** للتأكد من أن الخدمة تعمل
4. احفظ الإعدادات

#### ج. ربط الجهاز الشخصي
1. في صفحة **WhatsApp Web**، اضغط **"ربط WhatsApp Web"**
2. ستظهر صفحة مع QR Code
3. افتح WhatsApp على هاتفك:
   - **Android/iOS:** الإعدادات → الأجهزة المرتبطة → ربط جهاز
   - امسح QR Code
4. بعد المسح، سيتم حفظ الجلسة تلقائياً

### 3. بعد نجاح الربط

#### أ. التحقق من الحالة
- في صفحة **WhatsApp Web**، ستظهر حالة الاتصال:
  - ✅ **متصل بنجاح** - مع اسم ورقم الهاتف
  - ⚠️ **غير متصل** - إذا لم يتم الربط بعد

#### ب. إرسال الرسائل
الآن يمكنك إرسال الرسائل من:
- **WhatsApp → الرسائل → إرسال رسالة**
- **WhatsApp → الرسائل → إرسال جماعي**

الرسائل ستُرسل عبر جهازك الشخصي المرتبط.

#### ج. إعدادات الفواصل الزمنية
في صفحة **WhatsApp Web**، يمكنك تعديل:
- **الفاصل بين الرسائل:** الوقت بين كل رسالة (افتراضي: 3 ثواني)
- **الفاصل بين عمليات الإرسال الجماعي:** الوقت بين كل عملية إرسال جماعي (افتراضي: 5 ثواني)
- **الحد الأقصى للرسائل في الدقيقة:** (افتراضي: 20)
- **تفعيل الفواصل العشوائية:** لإضافة تباين عشوائي

**نصيحة:** اترك القيم الافتراضية لتجنب الحظر.

## كيفية عمل النظام بعد الربط

### 1. حفظ الجلسة
- بعد الربط الناجح، يتم حفظ الجلسة في قاعدة البيانات
- الجلسة تُحفظ أيضاً في مجلد `sessions/` في Node.js service
- الجلسة تبقى نشطة حتى تقطع الاتصال يدوياً

### 2. إرسال الرسائل
عند إرسال رسالة:
1. Laravel يرسل طلب إلى Node.js service
2. Node.js service يستخدم الجلسة المحفوظة
3. الرسالة تُرسل عبر WhatsApp Web المرتبط
4. يتم حفظ سجل الرسالة في قاعدة البيانات

### 3. الفواصل الزمنية
- عند الإرسال الجماعي، يتم تطبيق الفواصل الزمنية تلقائياً
- كل رسالة تُرسل مع فاصل زمني (مع تباين عشوائي إذا كان مفعلاً)
- هذا يحدث في الخلفية عبر Laravel Queue

### 4. إدارة الجلسات
- يمكنك رؤية حالة الجلسة في صفحة **WhatsApp Web**
- يمكنك قطع الاتصال من نفس الصفحة
- يمكنك إعادة الربط في أي وقت

## استكشاف الأخطاء

### المشكلة: "Browser initialization error"
**الحل:**
1. تأكد من أن Node.js service يعمل
2. راجع ملف `TROUBLESHOOTING.md` في `whatsapp-web-service`
3. جرب استخدام Chrome المثبت على النظام بدلاً من Chromium

### المشكلة: "لا يمكن الاتصال بخدمة Node.js"
**الحل:**
1. تأكد من أن الخدمة تعمل: `curl http://localhost:3000/health`
2. تحقق من رابط Node.js service في الإعدادات
3. تحقق من API Token (يجب أن يكون مطابقاً)

### المشكلة: QR Code لا يظهر
**الحل:**
1. تحقق من console logs في Node.js service
2. تأكد من أن Chromium مثبت بشكل صحيح
3. جرب حذف مجلد `sessions` وإعادة المحاولة

### المشكلة: الرسائل لا تُرسل
**الحل:**
1. تأكد من أن الجلسة متصلة (status: connected)
2. تحقق من تنسيق رقم الهاتف (يجب أن يكون بالصيغة الدولية: +966xxxxxxxxx)
3. راجع logs في Node.js service و Laravel

## ملاحظات مهمة

1. **الجلسة تبقى نشطة:** حتى بعد إغلاق المتصفح، الجلسة تبقى نشطة في Node.js service
2. **إعادة الاتصال:** إذا انقطع الاتصال، يمكنك إعادة الربط بسهولة
3. **الأمان:** API Token يحمي Node.js service من الوصول غير المصرح به
4. **الفواصل الزمنية:** مهمة جداً لتجنب الحظر من WhatsApp
5. **الخلفية:** جميع عمليات الإرسال تعمل في الخلفية ولا توقف النظام

## الخطوات التالية

بعد نجاح الإعداد:
1. ✅ Node.js service يعمل
2. ✅ Laravel متصل بـ Node.js service
3. ✅ الجهاز الشخصي مربوط
4. ✅ يمكنك إرسال الرسائل الآن!

**استمتع بإرسال الرسائل عبر WhatsApp Web! 🎉**


# خطة ربط تيليجرام بالمنصة

## نظرة عامة

الهدف: إضافة تيليجرام كقناة اتصال موازية للواتساب بحيث يمكن للمنصة (دورات، طلاب، إشعارات، إرسال جماعي) الاستفادة من البوت والقنوات مع الحفاظ على تناسق التجربة مع ما هو موجود في الواتساب حيث يناسب.

---

## الميزات المقترحة (حسب الأولوية)

### المرحلة 1 — الأساس (إرسال + ربط المستخدمين)

| الميزة | الوصف | الفائدة |
|--------|--------|---------|
| **إعدادات تيليجرام** | صفحة في لوحة التحكم: Bot Token، تفعيل/تعطيل، خيار "إرسال تيليجرام مع الواتساب أو بديلاً عنه" | التحكم المركزي واختيار القناة |
| **ربط حساب تيليجرام بالمستخدم** | حقل `telegram_chat_id` (وربما `telegram_username`) في جدول المستخدمين؛ صفحة للطالب لربط حسابه عبر رابط البوت أو إدخال معرف الدردشة | ربط الطالب لاستقبال الرسائل على تيليجرام |
| **إرسال رسالة واحدة (تيليجرام)** | خدمة ترسل نص/صورة/ملف إلى `chat_id` معين (مثل SendWhatsAppMessage لكن لتيليجرام) | إشعارات فردية، ردود تلقائية |
| **إرسال جماعي عبر تيليجرام** | نفس منطق البث الحالي (دورة/مجموعة) لكن للمستلمين الذين لديهم `telegram_chat_id` مع تأخير بين الرسائل (مثل WhatsApp broadcast) | إعلانات جماعية، تذكيرات للدورات |
| **قوالب رسائل تيليجرام** | قوالب نصوص جاهزة (مع placeholders مثل الاسم، اسم الدورة) قابلة للاختيار عند الإرسال الجماعي أو الفردي | توحيد الصياغة وتوفير الوقت |

### المرحلة 2 — استقبال الرسائل والتفاعل

| الميزة | الوصف | الفائدة |
|--------|--------|---------|
| **ويب هوك تيليجرام (Webhook)** | استقبال تحديثات البوت (رسائل واردة، /start، أزرار) على مسار مثل `/api/webhooks/telegram` | رد تلقائي، ربط الحساب عند /start |
| **ربط تلقائي عند /start** | المستخدم يرسل /start مع رابط يحتوي token (مثل `?start=link_xxx`)؛ البوت يربط `chat_id` بالمستخدم في المنصة | تجربة ربط سلسة للطالب |
| **رد تلقائي على الرسائل الواردة** | رسالة ثابتة أو قالب (مثل "تم استلام رسالتك، سنرد قريباً") مع خيار تفعيل/تعطيل من الإعدادات | تجربة مشابهة لـ auto_reply في الواتساب |
| **سجل المحادثات الواردة** | جدول لحفظ الرسائل الواردة من تيليجرام (user_id، chat_id، نص، تاريخ) وعرضها في لوحة التحكم | متابعة واسترجاع المحادثات |

### المرحلة 3 — إشعارات تلقائية ومتكاملة

| الميزة | الوصف | الفائدة |
|--------|--------|---------|
| **إشعارات تيليجرام للأحداث** | عند حدث (تسجيل في دورة، واجب جديد، إكمال درس، شهادة جاهزة) إرسال إشعار تيليجرام للطالب إذا كان مربوطاً وموافقاً في التفضيلات | زيادة التفاعل دون الاعتماد على البريد أو الواتساب فقط |
| **تفضيلات القناة (واتساب / تيليجرام)** | في `notification_preferences`: اختيار "إشعارات عبر تيليجرام" و/أو "إشعارات عبر واتساب" و/أو البريد | احترام رغبة المستخدم وتقليل الإزعاج |
| **إرسال مزدوج (واتساب + تيليجرام)** | في واجهة الإرسال الجماعي: خيار "إرسال إلى واتساب فقط" أو "تيليجرام فقط" أو "كلاهما" (مع مراعاة من لديه ربط لكل قناة) | مرونة في الحملات الإعلانية |
| **تنبيهات للإدمن عبر تيليجرام** | إشعار الإدمن عند طلب تسجيل جديد، طلب انضمام لمجموعة، أو أي حدث مهم (اختياري حسب إعدادات تيليجرام للإدمن) | متابعة فورية من الجوال |

### المرحلة 4 — قنوات ومجموعات (اختياري)

| الميزة | الوصف | الفائدة |
|--------|--------|---------|
| **ربط قناة تيليجرام (Channel)** | حفظ معرف القناة (@channel أو ID)؛ نشر من المنصة إلى القناة عند إعلان (مثلاً إعلان دورات أو منشور عام) | استخدام القناة كواجهة رسمية للمنصة |
| **مجموعة تيليجرام للدورة/المجموعة** | ربط مجموعة تيليجرام بدورة أو مجموعة تسجيل؛ إرسال إعلانات الدورة إلى المجموعة تلقائياً أو يدوياً | مجتمع مصغر لكل دورة |
| **دعوة تلقائية للمجموعة** | عند قبول الطالب في مجموعة التسجيل، إرسال رسالة تيليجرام تحتوي رابط الدعوة للمجموعة (إن وُجد ربط) | تسهيل انضمام الطلاب للمجموعات |

---

## البنية التقنية المقترحة

### قاعدة البيانات

- **جدول إعدادات تيليجرام (أو توسيع system_settings)**  
  مفاتيح مثل: `telegram_bot_token`, `telegram_enabled`, `telegram_auto_reply`, `telegram_auto_reply_message`, `telegram_send_with_whatsapp` (اختياري).

- **توسيع جدول المستخدمين (users)**  
  - `telegram_chat_id` (nullable, unique)  
  - `telegram_username` (nullable)  
  - (اختياري) `telegram_linked_at` للتدقيق.

- **جدول رسائل تيليجرام الواردة (telegram_incoming_messages)**  
  - user_id (nullable حتى يتم الربط), chat_id, text, update_id, received_at, read_at.

- **جدول بث تيليجرام (telegram_broadcasts)**  
  - نفس فكرة whatsapp_broadcasts: course_id, group_id, message, status, sent_count, failed_count, total_recipients, created_at.

- **جدول مستقبلو البث (telegram_broadcast_recipients)**  
  - broadcast_id, user_id, status (pending/sent/failed), sent_at, error_message.

- **جدول قوالب تيليجرام (telegram_message_templates)**  
  - name, body (نص مع placeholders), is_active.

- **(اختياري) ربط قنوات/مجموعات**  
  - جدول مثل `telegram_channel_links`: entity_type (course/group), entity_id, telegram_chat_id (قناة أو مجموعة), title.

### الملفات والخدمات (على غرار WhatsApp)

- **Config:** `config/telegram.php` — مفاتيح من env أو من system_settings.
- **Services:**  
  - `App\Services\Telegram\TelegramSettingsService` — قراءة/حفظ إعدادات تيليجرام.  
  - `App\Services\Telegram\SendTelegramMessage` — إرسال رسالة واحدة (نص، صورة، ملف) عبر Bot API.  
  - `App\Services\Telegram\BroadcastTelegramMessage` — تجهيز قائمة المستلمين (حسب دورة/مجموعة + وجود telegram_chat_id) واستدعاء الإرسال.
- **Jobs:**  
  - `App\Jobs\BroadcastTelegramMessageJob` — مشابه لـ BroadcastWhatsAppMessageJob مع تأخير تراكمي بين الرسائل.
- **Controllers:**  
  - `App\Http\Controllers\Admin\TelegramSettingsController` — صفحة إعدادات تيليجرام.  
  - `App\Http\Controllers\Admin\TelegramMessageController` أو توسيع `WhatsAppMessageController` — إرسال فردي/جماعي تيليجرام، قوالب، تقارير بث.  
  - `App\Http\Controllers\Api\TelegramWebhookController` — استقبال ويب هوك تيليجرام، ربط /start، حفظ الرسائل الواردة، رد تلقائي.
- **Models:**  
  - `TelegramBroadcast`, `TelegramBroadcastRecipient`, `TelegramMessageTemplate`, `TelegramIncomingMessage` (وما يلزم لربط القنوات لاحقاً).

### واجهات لوحة التحكم

- **إعدادات تيليجرام:** تحت "إعدادات" أو "الاتصالات": Bot Token، تفعيل البوت، تفعيل الرد التلقائي ونصه، خيار "إرسال تيليجرام مع الواتساب" إن وُجد.
- **ربط الطالب:** في صفحة الملف الشخصي للطالب (أو منطقة الطالب): "ربط تيليجرام" مع عرض رابط البوت + token، وحالة الربط (مرتبط/غير مرتبط).
- **إرسال رسائل تيليجرام:** صفحة أو تاب "تيليجرام" بجانب "واتساب" في قسم الرسائل: إرسال فردي، إرسال جماعي (دورة/مجموعة)، اختيار قالب، مع عرض عدد المستلمين الذين لديهم telegram_chat_id.
- **تقارير البث:** قائمة بثوط تيليجرام مع حالة كل بث ومستلم (مرسل/فاشل).
- **سجل الرسائل الواردة (اختياري):** صفحة تعرض آخر الرسائل الواردة من تيليجرام مع إمكانية التصفية حسب مستخدم/تاريخ.

### الأمان والخصوصية

- تخزين Bot Token مشفّراً (مثل api_token في الواتساب).
- التحقق من توقيع ويب هوك تيليجرام (secret_token) إن أمكن.
- ربط /start بـ token لمرة واحدة (one-time) لمنع ربط حساب من قبل شخص غير المالك.
- احترام تفضيلات المستخدم (إيقاف إشعارات تيليجرام من التفضيلات).

---

## ملخص المراحل والاعتماديات

```
المرحلة 1 (أساس)
├── إعدادات تيليجرام + Bot Token
├── telegram_chat_id (واختياري telegram_username) في users
├── SendTelegramMessage + BroadcastTelegramMessage + Job
├── واجهة إرسال جماعي تيليجرام (دورة/مجموعة)
└── قوالب رسائل تيليجرام

المرحلة 2 (ويب هوك وتفاعل)
├── TelegramWebhookController + route
├── ربط /start مع token
├── رد تلقائي
└── جدول telegram_incoming_messages + عرض في الأدمن

المرحلة 3 (إشعارات وتكامل)
├── تفضيلات القناة في notification_preferences
├── إشعارات تلقائية عند الأحداث (تسجيل، واجب، شهادة...)
├── خيار "واتساب و/أو تيليجرام" في الإرسال الجماعي
└── تنبيهات للإدمن

المرحلة 4 (قنوات ومجموعات — اختياري)
├── ربط قناة تيليجرام
├── ربط مجموعة بدورة/مجموعة
└── نشر ودعوات تلقائية
```

---

## الخطوة التالية

- البدء بالمرحلة 1: إعدادات، ربط المستخدم (telegram_chat_id)، إرسال فردي وجماعي مع تأخير بين الرسائل، وقوالب بسيطة.
- بعد استقرار المرحلة 1، إضافة ويب هوك وربط /start والرد التلقائي (المرحلة 2)، ثم إشعارات الأحداث والتكامل مع الواتساب (المرحلة 3).

يمكن استخدام هذا الملف كمرجع أثناء التطوير وتحديثه عند إضافة ميزات جديدة أو تغيير الأولويات.
